"""
UX-рецензент сайта — автономный агент.

Стопка слоёв (каждый опирается только на слой под собой):

    agent.py / app       — точки входа: командная строка и веб-сервис
        analyzer         — промпты и сборка отчёта (о HTTP не знает)
            llm_client   — общение с моделью (о UX не знает)
            fetcher      — загрузка и очистка страницы (о модели не знает)
                logging_setup — общий журнал для всех слоёв

Смысл разделения: дефект ищется бисекцией. Пустой отчёт при живой странице —
слой анализа; ошибка соединения — слой загрузки или клиент; и то и другое видно
в логе по имени слоя в каждой строке.
"""

__version__ = "1.0.0"
